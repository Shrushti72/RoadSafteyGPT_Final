[theme]
primaryColor="#005A9C"  # Updated deep blue for safety focus
backgroundColor="#F8FAFC"  # Light gray background
secondaryBackgroundColor="#E0F2FE"  # Light blue for better contrast
textColor="#0F172A"  # Darker gray for text
font="sans serif"
[runner]
# Forces Streamlit to use a modern, supported Python version (e.g., 3.11)
# which avoids the distutils/numpy build issues seen in the console.
python_version = "3.11"
