# --- Core Streamlit App Functionality (From app.py) ---
streamlit
pandas
Pillow
requests
python-dotenv
folium
streamlit-folium

# --- Core RAG and LLM Libraries ---
langchain==0.1.0
langchain-community==0.0.13
langchain-core>=0.1.7,<0.2
langchain-google-genai==3.0.0
google-generativeai
openai
tiktoken

# --- Pinned Versions for Build Stability ---
# Pydantic is a dependency of LangChain. Pinning it avoids conflicts.
pydantic==1.10.13
# numpy is the source of the distutils error. Pinning it to a stable, older version 
# that is known to build correctly on Python 3.10 is essential.
numpy==1.24.4 

# --- Vector Store & Simple Parsing ---
chromadb
pypdf 

# --- Removed Unused/Problematic Packages ---
# REMOVED: fastapi, uvicorn, nltk, lxml, unstructured[all-docs]
